# Configurazione di pytest per questo progetto didattico.
[pytest]

# Cerca i test solo nella cartella dedicata, evitando directory temporanee locali.
testpaths = tests

# Disattiva la cache di pytest: utile su Windows quando alcune cartelle temp non sono accessibili.
addopts = -p no:cacheprovider
